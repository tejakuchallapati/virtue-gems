/** Shared Next/Image settings for sharper jewellery photos on retina screens. */

/** Default quality for product photography (Next default 75 looks soft). */
export const PRODUCT_IMAGE_QUALITY = 90;

/** Shop / home / wishlist cards — 1-col tiny phones, 2-col small, up to 4-col desktop. */
export const PRODUCT_CARD_SIZES =
  "(max-width: 359px) 100vw, (max-width: 640px) 50vw, (max-width: 1024px) 33vw, 25vw";

/** Product detail hero — nearly full width on phones. */
export const PRODUCT_DETAIL_SIZES =
  "(max-width: 640px) 100vw, (max-width: 1024px) 90vw, 50vw";

/** Home trending / bestseller highlight tiles. */
export const PRODUCT_HIGHLIGHT_SIZES =
  "(max-width: 768px) 100vw, 224px";

/** Cart / wishlist row thumbnails. */
export const PRODUCT_THUMB_SIZES = "(max-width: 380px) 80px, (max-width: 640px) 96px, 112px";

/** Product detail gallery thumbs. */
export const PRODUCT_GALLERY_THUMB_SIZES = "80px";
